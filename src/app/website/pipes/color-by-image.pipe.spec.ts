import { ColorByImagePipe } from './color-by-image.pipe';

describe('ColorByImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorByImagePipe();
    expect(pipe).toBeTruthy();
  });
});
